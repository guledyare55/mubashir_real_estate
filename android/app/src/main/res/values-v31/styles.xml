<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- 
            We use our 'invisible' white icon on a white background to force a 
            pure white startup, effectively hiding the system's required icon.
        -->
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon_white</item>
        <item name="android:windowSplashScreenBackground">@android:color/white</item>
        
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
